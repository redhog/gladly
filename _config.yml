title: Gladly
description: A lightweight, GPU-accelerated plotting library with a declarative API
baseurl: /gladly/docs

theme: minima

plugins:
  - jekyll-optional-front-matter

# relative_links disabled - causes issues with GitHub Pages URL rewriting
relative_links:
  enabled: false
  collections: false

exclude:
  - node_modules
  - src
  - example
  - dist-example
  - scripts
  - Gemfile.lock
  - package.json
  - package-lock.json
  - CLAUDE.md

defaults:
  - scope:
      path: ""
    values:
      layout: default
  - scope:
      path: "README.md"
    values:
      permalink: /
  - scope:
      path: "docs"
    values:
      permalink: /:basename/
  - scope:
      path: "docs/architecture"
    values:
      permalink: /architecture/:basename/
